<%- include('partials/header') %>

<h1>Home</h1>
<p><%= homeText %></p>

<% postList.forEach(function(post){%>
    
    <h1><%= post.postT %></h1> 
    <p><%= post.postM.substring(0, 99) + ' ...' %>  <a href="<%= '/posts/'+post.postT %>">Read More</a></p> 

<% }); %>
    


<%- include('partials/footer') %>